import ModuleLoadingIndicator from '@/components/ModuleLoadingIndicator';
import { FC } from 'react';

const Loading: FC = () => {
  return <ModuleLoadingIndicator style={{ height: '100svh' }} />;
};

export default Loading;
